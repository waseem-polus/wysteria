import { WuiApp, Container, Button, TabGroup, Tab } from "@wysteria/design";
import ComponentPreview from "../../components/ComponentPreview";
import ButtonEditor from "../../components/ButtonEditor";
import Prop from "../../components/Prop";
import { Home, HandMetal } from "lucide-react";

# Button

An interactive element activated by a user with a mouse, keyboard, finger, voice command, or other assistive technology.

<ComponentPreview>
    <Container
        paddingVer="md"
        gap="sm"
        justify="space-evenly"
        align="center"
        direction="row"
        style={{ width: "100%" }}
    >
        <Button>
            <HandMetal size={20} /> {"Button"}
        </Button>
    </Container>
</ComponentPreview>

```jsx copy
<Button>
    <HandMetal size={20} /> Button
</Button>
```

## Examples

### Icon

  <TabGroup>
    <Tab label="Preview">
      <Container paddingVer="md" gap="sm" justify="space-evenly" align="center" direction="row" style={{minHeight: "5rem"}}>
        <Button size="icon"><Home size={20} /></Button>
        <Button size="icon" variant="outline"><Home size={20} /></Button>
        <Button size="icon" variant="ghost"><Home size={20} /></Button>
        <Button size="icon" variant="text"><Home size={20} /></Button>
      </Container>
    </Tab>

    <Tab label="Code">
      ```js
      <Button size="icon">
        <Home size={20} />
      </Button>

      <Button size="icon" variant="outline">
        <Home size={20} />
      </Button>

      <Button size="icon" variant="ghost">
        <Home size={20} />
      </Button>

      <Button size="icon" variant="text">
        <Home size={20} />
      </Button>
      ```
    </Tab>

  </TabGroup>

### Destructive

  <TabGroup>
    <Tab label="Preview">
      <Container paddingVer="md" gap="sm" justify="space-evenly" align="center" direction="row" style={{minHeight: "5rem"}}>
        <Button action="destructive"> Button </Button>
      </Container>

    </Tab>

    <Tab label="Code">
      ```jsx
        <Button action="destructive">
          Button
        </Button>
      ```
    </Tab>

  </TabGroup>

### Neutral

  <TabGroup>
    <Tab label="Preview">
      <Container paddingVer="md" gap="sm" justify="space-evenly" align="center" direction="row" style={{minHeight: "5rem"}}>
        <Button action="neutral"> Button </Button>
      </Container>

    </Tab>

    <Tab label="Code">
      ```jsx
        <Button action="neutral">
          Button
        </Button>
      ```
    </Tab>

  </TabGroup>

### Outline

  <TabGroup>
    <Tab label="Preview">
      <Container paddingVer="md" gap="sm" justify="space-evenly" align="center" direction="row" style={{minHeight: "5rem"}}>
        <Button variant="outline"> Button </Button>
      </Container>

    </Tab>

    <Tab label="Code">
      ```jsx
        <Button variant="outline">
          Button
        </Button>
      ```
    </Tab>

  </TabGroup>

### Ghost 

<TabGroup>
  <Tab label="Preview">
    <Container paddingVer="md" gap="sm" justify="space-evenly" align="center" direction="row" style={{minHeight: "5rem"}}>
      <Button variant="ghost">
        Button
      </Button>
    </Container>

  </Tab>

<Tab label="Code">
    ```jsx
    <Button variant="ghost">
      Button
    </Button>
    ```
</Tab>

</TabGroup>

### Text

  <TabGroup>
    <Tab label="Preview">
      <Container paddingVer="md" gap="sm" justify="space-evenly" align="center" direction="row" style={{minHeight: "5rem"}}>
        <Button variant="text"> Button </Button>
      </Container>

    </Tab>

    <Tab label="Code">
      ```jsx
        <Button variant="text">
          Button
        </Button>
      ```
    </Tab>

  </TabGroup>

### Disabled

  <TabGroup>
    <Tab label="Preview">
      <Container paddingVer="md" gap="sm" justify="space-evenly" align="center" direction="row" style={{minHeight: "5rem"}}>
        <Button disabled> Button </Button>
      </Container>

    </Tab>

    <Tab label="Code">
      ```jsx
        <Button disabled>
          Button
        </Button>
      ```
    </Tab>

  </TabGroup>

## API Reference

| Prop                   | Type                                  |    Default    |
| ---------------------- | ------------------------------------- | :-----------: |
| <Prop>variant</Prop>   | `filled` `ghost` `outline` `text`     |   `filled`    |
| <Prop>action</Prop>    | `progressive` `destructive` `neutral` | `progressive` |
| <Prop>size</Prop>      | `normal` `icon`                       |   `normal`    |
| <Prop>disabled</Prop>  | `boolean`                             |    `false`    |
| <Prop>onClick</Prop>   | `() => void`                          |       -       |
| <Prop>className</Prop> | `string`                              |       -       |
