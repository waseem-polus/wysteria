import ComponentPreview from "../../components/ComponentPreview";
import {
    Dialog,
    DialogTrigger,
    DialogContent,
    DialogTitle,
    DialogDescription,
    DialogFooter,
    DialogClose,
    Button,
    Link,
} from "@wysteria/design";

# Dialog

A window overlaid on either the primary window or another dialog window, rendering the content underneath inert.

<Link target="_blank" link="https://www.radix-ui.com/primitives/docs/components/dialog">
    API Reference
</Link>

<ComponentPreview>
    <Dialog modal>
        <DialogTrigger>
            <Button leftIcon="User">Edit Profile</Button>
        </DialogTrigger>
        <DialogContent>
            <DialogClose />
            <DialogTitle>Edit profile</DialogTitle>
            <DialogDescription>
                Make changes to your profile here. Click save when you're done.
            </DialogDescription>
            <DialogFooter>
                <DialogClose>
                    <Button variant="text">Cancel</Button>
                </DialogClose>
                <Button>Save</Button>
            </DialogFooter>
        </DialogContent>
    </Dialog>
</ComponentPreview>

```jsx copy
<Dialog modal>
    <DialogTrigger>
        <Button leftIcon="User">Edit Profile</Button>
    </DialogTrigger>
    <DialogContent>
        <DialogClose />
        <DialogTitle>Edit profile</DialogTitle>
        <DialogDescription>
            Make changes to your profile here. Click save when you're done.
        </DialogDescription>
        <DialogFooter>
            <DialogClose>
                <Button variant="text">Cancel</Button>
            </DialogClose>
            <Button>Save</Button>
        </DialogFooter>
    </DialogContent>
</Dialog>
```
