import {
    WuiApp,
    Chip,
    Input,
    InputIcon,
    Button,
    Container,
    TabGroup,
    Tab,
    TextField,
    NumberField,
    PasswordField,
} from "@wysteria/design";
import { ComponentPreview, Prop } from "../../components";
import { Search } from "lucide-react";

# Input

An interactive form element used to accept data from the user.

<ComponentPreview>
    <Container
        paddingVer="md"
        gap="sm"
        justify="center"
        align="center"
        direction="row"
        style={{ width: "100%" }}
    >
        <Input placeholder="Text Input" />
    </Container>
</ComponentPreview>
```jsx
<Input placeholder="Text Input" />
```

## Examples

### Number

<TabGroup>
    <Tab label="Preview">
        <Container paddingVer="md" gap="sm" justify="space-evenly" align="center" direction="row" style={{minHeight: "5rem"}}>
            <Input type="number" placeholder="Number Input" />
        </Container>
    </Tab>

    <Tab label="Code">
        ```js
        <Input type="number" placeholder="Number Input" />
        ```
    </Tab>

</TabGroup>

### Email

<TabGroup>
    <Tab label="Preview">
        <Container paddingVer="md" gap="sm" justify="space-evenly" align="center" direction="row" style={{minHeight: "5rem"}}>
            <Input type="email" placeholder="Email Input" />
        </Container>
    </Tab>

    <Tab label="Code">
        ```js
        <Input type="email" placeholder="Email Input" />
        ```
    </Tab>

</TabGroup>

### Password

<TabGroup>
    <Tab label="Preview">
        <Container paddingVer="md" gap="sm" justify="space-evenly" align="center" direction="row" style={{minHeight: "5rem"}}>
            <Input type="password" placeholder="Password Input" />
        </Container>
    </Tab>

    <Tab label="Code">
        ```js
        <Input type="password" placeholder="Password Input" />
        ```
    </Tab>

</TabGroup>

### Tel

<TabGroup>
    <Tab label="Preview">
        <Container paddingVer="md" gap="sm" justify="space-evenly" align="center" direction="row" style={{minHeight: "5rem"}}>
            <Input type="tel" placeholder="Phone Input" />
        </Container>
    </Tab>

    <Tab label="Code">
        ```js
        <Input type="tel" placeholder="Phone Input" />
        ```
    </Tab>

</TabGroup>

### URL

<TabGroup>
    <Tab label="Preview">
        <Container paddingVer="md" gap="sm" justify="space-evenly" align="center" direction="row" style={{minHeight: "5rem"}}>
            <Input type="url" placeholder="URL Input" />
        </Container>
    </Tab>

    <Tab label="Code">
        ```js
        <Input type="url" placeholder="URL Input" />
        ```
    </Tab>

</TabGroup>

### Search

<TabGroup>
    <Tab label="Preview">
        <Container paddingVer="md" gap="sm" justify="space-evenly" align="center" direction="row" style={{minHeight: "5rem"}}>
            <Input type="search" placeholder="Seach Input" />
        </Container>
    </Tab>

    <Tab label="Code">
        ```js
        <Input type="search" placeholder="Seach Input" />
        ```
    </Tab>

</TabGroup>

### With Icon

<TabGroup>
    <Tab label="Preview">
        <Container paddingVer="md" gap="sm" justify="space-evenly" align="center" direction="row" style={{minHeight: "5rem"}}>
            <Input type="search" placeholder="Seach...">
                <InputIcon>
                    <Search />
                </InputIcon>
            </Input>
        </Container>
    </Tab>

    <Tab label="Code">
        ```js
        <Input type="search" placeholder="Seach...">
            <InputIcon>
                <Search />
            </InputIcon>
        </Input>
        ```
    </Tab>

</TabGroup>

### Disabled

<TabGroup>
    <Tab label="Preview">
        <Container paddingVer="md" gap="sm" justify="space-evenly" align="center" direction="row" style={{minHeight: "5rem"}}>
            <Input placeholder="Disabled Input" disabled />
        </Container>
    </Tab>

    <Tab label="Code">
        ```js
        <Input placeholder="Disabled Input" disabled />
        ```
    </Tab>

</TabGroup>

## API Reference

| Prop                     | Type      | Default |
| ------------------------ | --------- | :-----: |
| <Prop>placeholder</Prop> | `string`  |    -    |
| <Prop>value</Prop>       | `string`  |    -    |
| <Prop>indicator</Prop>   | `boolean` | `false` |
| <Prop>caption</Prop>     | `boolean` | `false` |
| <Prop>error</Prop>       | `boolean` | `false` |
